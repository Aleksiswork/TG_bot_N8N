# 🤖 Telegram Bot v3.1

Улучшенный Telegram бот для распространения гайдов и файлов с системой обратной связи.

## ✨ Возможности

### 📚 Для пользователей
- **Гайды и материалы** - доступ к полезным инструкциям
- **Обратная связь** - отправка сообщений администрации с файлами
- **История обращений** - просмотр своих предыдущих сообщений
- **Подписка на канал** - обязательная подписка для доступа

### ⚙️ Для администраторов
- **Статистика** - информация о пользователях и активности
- **Управление обращениями** - просмотр, ответы, удаление
- **🚫 Система блокировок** - управление заблокированными пользователями
- **Рассылка** - отправка сообщений всем пользователям
- **Экспорт данных** - выгрузка пользователей в CSV
- **Мониторинг** - логирование всех действий

## 🚀 Установка и настройка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd TG_bot_N8N
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения
Создайте файл `.env` в корневой папке:

```env
# Telegram settings
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321

# Channel settings
CHANNEL_USERNAME=your_channel_username
CHANNEL_LINK=https://t.me/your_channel

# Files settings
FILES_DIR=files
DB_USERS_PATH=data/users.db
DB_SUBMISSIONS_PATH=data/submissions.db

# Bot settings
LOG_LEVEL=INFO
MAX_FILE_SIZE_MB=50
MAX_FILES_PER_SUBMISSION=5
MAX_SUBMISSION_LENGTH=4000
```

### 4. Запуск бота

#### Способ 1: Прямой запуск
```bash
python main.py
```

#### Способ 2: Через wrapper-скрипт (рекомендуется)
```bash
python run_bot.py
```

#### Способ 3: Через простой скрипт (если зависает консоль)
```bash
python run_simple.py
```

**Преимущества wrapper-скриптов:**
- ✅ Корректная обработка Ctrl+C
- ✅ Не "зависает" консоль после остановки
- ✅ Автоматическая очистка ресурсов
- ✅ Лучшая совместимость с Windows
- ✅ Принудительное завершение при необходимости

## 📁 Структура проекта

```
TG_bot_N8N/
├── config.py              # Конфигурация бота
├── main.py                # Точка входа
├── database/              # База данных
│   ├── __init__.py
│   ├── db.py              # Основная БД пользователей
│   ├── submissions.py     # БД обращений
│   └── banned.py          # БД заблокированных пользователей
├── handlers/              # Обработчики команд
│   ├── __init__.py
│   ├── common.py          # Общие команды
│   ├── user.py            # Пользовательские функции
│   └── admin.py           # Админские функции
├── keyboards/             # Клавиатуры
│   ├── __init__.py
│   ├── base.py            # Базовые клавиатуры
│   └── admin.py           # Админские клавиатуры
├── utils/                 # Утилиты
│   ├── __init__.py
│   └── checks.py          # Проверки и валидация
├── files/                 # Папка для файлов
├── data/                  # Базы данных
└── README.md              # Документация
```

## 🔧 Конфигурация

### Обязательные параметры
- `BOT_TOKEN` - токен бота от @BotFather
- `ADMIN_IDS` - ID администраторов через запятую
- `DB_USERS_PATH` - путь к БД пользователей

### Опциональные параметры
- `CHANNEL_USERNAME` - username канала для подписки
- `CHANNEL_LINK` - ссылка на канал
- `FILES_DIR` - папка для файлов (по умолчанию: files)
- `LOG_LEVEL` - уровень логирования (по умолчанию: INFO)
- `MAX_FILE_SIZE_MB` - максимальный размер файла (по умолчанию: 50MB)
- `MAX_FILES_PER_SUBMISSION` - максимум файлов на сообщение (по умолчанию: 5)
- `MAX_SUBMISSION_LENGTH` - максимальная длина текста (по умолчанию: 4000)

## 📱 Команды бота

### Пользовательские команды
- `/start` - Запуск бота и главное меню
- `/help` - Справка по командам
- `/status` - Статус бота и профиль
- `/guides` - Доступ к гайдам
- `/feedback` - Обратная связь

### Админские команды
- `/admin` - Админ панель
- `/stats` - Статистика

## 🗄️ База данных

### Таблица users
- `user_id` - ID пользователя (PRIMARY KEY)
- `username` - Username пользователя
- `first_name` - Имя
- `last_name` - Фамилия
- `created_at` - Дата регистрации
- `last_active` - Последняя активность

### Таблица submissions
- `id` - ID обращения (PRIMARY KEY)
- `user_id` - ID пользователя
- `username` - Username пользователя
- `text_content` - Текст обращения
- `file_ids` - JSON с ID файлов
- `status` - Статус (new, viewed, solved)
- `created_at` - Дата создания
- `admin_response` - Ответ администратора

### Таблица banned_users
- `user_id` - ID пользователя (PRIMARY KEY)
- `username` - Username пользователя
- `banned_at` - Дата блокировки
- `banned_by` - ID администратора
- `reason` - Причина блокировки
- `expires_at` - Дата истечения (NULL для постоянных)
- `is_permanent` - Постоянная блокировка
- `ban_count` - Количество блокировок
- `last_ban_reason` - Причина последней блокировки

## 🔒 Безопасность

- Проверка подписки на канал
- Валидация размера файлов
- Ограничение длины сообщений
- Проверка прав администратора
- Логирование всех действий
- **🚫 Система блокировок** - защита от спама и нарушений
  - Автоматическая блокировка при спаме (>5 сообщений/мин)
  - Блокировка за дублирование сообщений (>3 раза)
  - Прогрессивные санкции: 24ч → 7 дней → навсегда
  - Автоматическая очистка истекших блокировок

## 📊 Логирование

Бот ведет подробные логи в файле `bot.log`:
- Информация о запуске/остановке
- Действия пользователей
- Ошибки и исключения
- Админские операции

## 🚀 Развертывание

### Локальный запуск
```bash
python main.py
```

### Запуск в фоне (Linux)
```bash
nohup python main.py > bot.log 2>&1 &
```

### Docker (опционально)
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "main.py"]
```

## 🔄 Обновления

### v3.1 (текущая)
- ✅ **Система блокировок** - прогрессивные санкции (24ч → 7 дней → навсегда)
- ✅ **Автоматическая блокировка** - защита от спама и дублирования сообщений
- ✅ **Админ-панель блокировок** - управление заблокированными пользователями
- ✅ **Правила обратной связи** - четкие инструкции для пользователей
- ✅ Исправлена проблема с "зависанием" консоли
- ✅ Добавлен wrapper-скрипт для корректного запуска
- ✅ Улучшена обработка сигналов остановки
- ✅ Оптимизирована очистка ресурсов

### v3.0
- ✅ Улучшенная архитектура
- ✅ Новая система конфигурации
- ✅ Расширенные утилиты
- ✅ Улучшенные клавиатуры
- ✅ Подробная документация

### v2.0
- ✅ Модульная структура
- ✅ Система обратной связи
- ✅ Множественные админы

## 🔧 Решение проблем

### Проблема: Консоль "зависает" после остановки бота
**Симптомы:** После нажатия Ctrl+C консоль не отвечает на ввод команд

**Решение:**
1. Используйте простой скрипт: `python run_simple.py` (рекомендуется)
2. Или wrapper-скрипт: `python run_bot.py`
3. Или запускайте с флагом: `python main.py --simple`
4. В крайнем случае, закройте терминал и откройте новый

### Проблема: Бот не запускается
**Проверьте:**
1. Правильность токена в `.env`
2. Наличие всех зависимостей: `pip install -r requirements.txt`
3. Права доступа к папкам `files/` и `data/`

### Проблема: Ошибки с базой данных
**Решение:**
1. Удалите файлы `data/users.db` и `data/submissions.db`
2. Перезапустите бота - таблицы создадутся автоматически

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи в файле `bot.log`
2. Убедитесь в правильности настроек в `.env`
3. Проверьте права доступа к папкам
4. Обратитесь к администратору через бота

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

**Создано с ❤️ для удобства пользователей**
